export declare const credentials: {
    local: {
        databaseConnections: {
            postgres: {
                database: string;
                username: string;
                password: string;
                params: {
                    dialect: string;
                    host: string;
                    port: number;
                };
            };
        };
    };
};
